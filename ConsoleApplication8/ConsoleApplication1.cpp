#include <iostream>
#include <Windows.h>
using namespace std;

int main()
{
    SetConsoleOutputCP(1251);
    SetConsoleCP(1251);

    // Статичний масив - це змінна, що зберігає ОДНОТИПНІ дані ФІКСОВАНОЇ довжини.
    // Статичний массив може зберігати тільки один тип даних
    // Статичний массив не може бути збільшений чи зменшений після створення
    
    const int n = 3;

    int arr[n];
    
}
